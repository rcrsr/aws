{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"${CLAUDE_PROJECT_DIR}\" && node ${CLAUDE_PLUGIN_ROOT}/scripts/awsx.mjs guard"
          }
        ]
      }
    ]
  }
}
